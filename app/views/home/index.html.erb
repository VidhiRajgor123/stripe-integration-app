<!DOCTYPE html>
<html>
  <head>
    <style>
      .logout-link {
        position: absolute;
        top: 10px;
        right: 20px;
        font-weight: bold;
        font-size: x-large;
      }
    </style>
  </head>
  <body>
    <h1>Demo Application</h1>

    <% if user_signed_in? %>
      <%= link_to "Logout", destroy_user_session_path, method: :delete, class: "logout-link" %>
    <% end %>
  </body>
</html>
